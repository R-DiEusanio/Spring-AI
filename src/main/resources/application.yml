spring:
  application:
    name: deepseek-demo

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: deepseek-r1:1.5b
      embedding:
        model: nomic-embed-text:latest
    vectorstore:
      pgvector:
        dimensions: 768
        initialize-schema: true
        index-type: HNSW

  datasource:
    url: jdbc:postgresql://localhost:5433/vector_database
    username: postgres
    password: Riccardo2001
    driver-class-name: org.postgresql.Driver

server:
  port: 8084
